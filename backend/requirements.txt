fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12

# Phase 1: Database
sqlalchemy>=2.0.36
alembic>=1.14.0

# Phase 1: Validation & Schemas
pydantic[email]>=2.10.0

# Phase 1: Authentication & Security
python-jose[cryptography]>=3.3.0
cryptography>=43.0.0
passlib[bcrypt]>=1.7.4

# Phase 1: OAuth & HTTP
requests>=2.32.0
httpx>=0.27.0

# Phase 1: Database Drivers
psycopg[binary]>=3.2.3  # PostgreSQL (psycopg3 — has Python 3.14 wheels)

# Phase 4: Scheduler (APScheduler) — gated by SCHEDULER_ENABLED env
apscheduler>=3.10.4

